[project]
name = "langchain-harness-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ronny Camacho", email = "rac.ia.2025@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "langchain-google-genai>=4.4.0",
    "langchain[google-genai]>=1.4.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
langchain-harness-2026 = "langchain_harness_2026:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
